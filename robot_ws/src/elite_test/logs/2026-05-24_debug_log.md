
# 机械臂串口通信调试日志 — 2026-05-24

## 成果

1. **MoveIt2 配置修复** — 关节类型、限位、自碰撞、运动学、控制器
2. **PCA9685 舵机驱动** — I2C3 50Hz PWM，0~180° 角度转换
3. **ROS2 → F407 串口指令下发** — 关节角度协议帧 (type 0x20)
4. **F407 接收解析** — 逐字节中断接收，帧解析，舵机执行
5. **Motion Planning 节点** — 纯 ROS2 Action 接口，无 moveit_commander 依赖
6. **抓取测试程序** — 7 步关节序列，串口下发

---

## 问题与修复

### 1. URDF 关节类型错误
- **现象**: MoveIt 报 `continuous` joint 无限制
- **修复**: arm.urdf 6 处 `type="continuous"` → `"revolute"`
- **文件**: `arm_ros2/urdf/arm.urdf`

### 2. 自碰撞误报
- **现象**: `base_link` 与 `arm_Link1` 碰撞
- **修复**: arm.srdf 添加 8 对 `disable_collisions`
- **文件**: `elite_test/config/arm.srdf`

### 3. joint_limits.yaml 类型错误
- **现象**: MoveIt 崩溃 `expected [double] got [integer]`
- **修复**: `max_velocity: 1` → `1.0`；添加 `min_position/max_position`
- **文件**: `elite_test/config/joint_limits.yaml`

### 4. SRDF virtual_joint 循环引用
- **现象**: `parent_frame="base_link" child_link="base_link"`
- **修复**: `parent_frame` → `"world"`
- **文件**: `elite_test/config/arm.srdf`

### 5. KDL 运动学超时
- **现象**: 规划结果 0 航点，`planning_time: 0.0`
- **修复**: `kinematics_solver_timeout: 0.005` → `0.05`
- **文件**: `elite_test/config/kinematics.yaml`

### 6. BT Navigator 插件缺失
- **现象**: `libnav2_are_error_codes_active_condition_bt_node.so` 不存在
- **修复**: 移除该插件 + 修复 BT XML 路径
- **文件**: `alloy_chassis_mecanum/config/nav2_params.yaml`

### 7. moveit_commander 不存在 (Humble)
- **现象**: `ModuleNotFoundError: No module named 'moveit_commander'`
- **修复**: 改用 `rclpy.action.ActionClient` + `moveit_msgs/action/MoveGroup`
- **文件**: `elite_test/scripts/arm_fixed_grasp_test.py`

### 8. ros2_control 控制器加载失败
- **现象**: `The 'type' param was not defined for 'arm_group_controller'`
- **修复**: 基于 `demo.launch` 而非手动组装
- **文件**: `elite_test/launch/test_grasp.launch.py`

### 9. MoveGroup action 回调死锁 (核心调试)
- **现象**: `send_goal_async` future 永远不完成
- **原因**: 在 timer 回调中调用 `spin_until_future_complete` 导致 executor 死锁
- **修复**: 改为在 `main()` 中直接调用（spin 前执行序列）
- **文件**: `elite_test/scripts/arm_fixed_grasp_test.py`

### 10. 串口指令不执行 (核心问题，耗时最长)
- **现象**: Python 发送帧正确，F407 运行正常，但舵机不动
- **调试过程**:
  1. 怀疑 I2C 地址 → 验证 STM32F4 HAL 需 0x80 ✅（无误）
  2. 怀疑 USART 端口 → 改 USART1 ↔ USART2 反复验证
  3. 怀疑 printf 缓冲 → 加 fflush、改 \r\n
  4. 怀疑 DMA 空闲中断 → 注释 ROS2 TX 测试，仍不工作
  5. 怀疑帧解析器 → 发现 ParseVelocityFrame 对非匹配类型返回 1，吞掉关节帧
  - **修复**: 非匹配类型返回 `consumed=0` 让下个解析器处理
  - **文件**: `Hardware/ros2_uart.c`
  6. 仍不工作 → 发现 DMA 空闲中断在 USART2 全双工时不可靠
  - **根因**: `HAL_UARTEx_ReceiveToIdle_DMA` 在 USART2 同时 TX/RX 时空闲检测异常
  - **修复**: 改为 `HAL_UART_Receive_IT` 逐字节中断接收
  - **文件**: `Core/Src/main.c`
  7. ✅ 舵机成功响应

### 11. URDF 文件截断
- **现象**: 编译错误 `unclosed token`
- **修复**: 补回 `</joint>` `</robot>` 闭合标签
- **文件**: `arm_ros2/urdf/arm.urdf`

---

## 最终架构

```
┌─────────────────────────────────────────────────────────────┐
│ ROS2 (RDK X5)                                               │
│                                                              │
│  arm_fixed_grasp_test.py ──→ MoveGroup action ──→ move_group │
│         │                                                  │
│         └──→ SerialJointSender ──→ /dev/ttyACM0             │
│                   帧: [AA][20][18 00][6×float][55]          │
└──────────────────────┬──────────────────────────────────────┘
                       │ USB CDC (DAPLink)
┌──────────────────────┴──────────────────────────────────────┐
│ STM32F407                                                    │
│                                                              │
│  USART2 RX (PA3)                                             │
│    │ HAL_UART_Receive_IT (逐字节中断)                        │
│    ▼                                                         │
│  HAL_UART_RxCpltCallback                                     │
│    │                                                         │
│    ▼                                                         │
│  ProcessROS2Command()                                        │
│    ├── ParseVelocityFrame (type 0x10) → 电机控制             │
│    └── ParseJointAngleFrame (type 0x20) → PCA9685            │
│                                              │               │
│  I2C3 (PC9 SDA, PA8 SCL)                    │               │
│    │                                         ▼               │
│    └── PCA9685 ← SetJointAngles(rad→deg→PWM)               │
│           │                                                  │
│      ch0~ch5 → 6× 舵机 (0~180°)                             │
└──────────────────────────────────────────────────────────────┘
```

## 关键调试经验

1. **DMA 空闲中断不可靠**: STM32F4 上 USART2 同时 TX/RX 时，`HAL_UARTEx_ReceiveToIdle_DMA` 的空闲检测可能失效。**逐字节中断接收 (`HAL_UART_Receive_IT`)** 更可靠。

2. **帧解析器不要吞非匹配帧**: 类型不匹配时返回 `consumed=0` 让下个解析器重试，而非 `consumed=total_len`。

3. **ROS2 action 避免在回调中同步等待**: `spin_until_future_complete` 在 executor 回调中会死锁。在 `main()` 中调用（spin 前）或使用回调模式。

4. **USB CDC 缓冲小**: DAPLink VCP 的 USB 缓冲只有 ~512 字节，启动消息会被后续数据快速覆盖。不依赖启动阶段的 printf 调试。

5. **引脚确认是关键**: DAPLink VCP 实际连接的是 USART2 (PA2/PA3)，而非 USART1 (PA9/PA10)。通过分析串口输出中的 ROS2 帧（0xAA 0x01）来确认。

---

## 补充: RViz ↔ 真实机械臂桥接 (2026-05-25)

### 问题
RViz MotionPlanning 的 Plan & Execute 只控制虚拟机械臂（FakeSystem），真实舵机不动。

### 修复
新增 `arm_bridge.py` — 订阅 `/joint_states`，10Hz 节流，变化时通过串口下发 F407。

### 文件
- `elite_test/scripts/arm_bridge.py` — 桥接节点
- `elite_test/CMakeLists.txt` — 注册脚本

### 使用方法
```bash
# 终端 1
ros2 launch elite_test demo.launch.py
# 终端 2
ros2 run elite_test arm_bridge.py
```

### 数据流
```
RViz 滑块 → MoveGroup → ros2_control(Fake) → /joint_states → arm_bridge → 串口 → F407 → PCA9685 → 舵机
```

### 位姿规划问题
KDL IK 求解器对该机械臂的位姿目标全部返回 FAILURE (err=99999)。原因是 joint origin 有复杂 rpy 旋转，KDL 迭代不稳定。关节空间控制完全正常。


---

## 补充: RViz 控制启动 (2026-05-25 20:28)

### 操作
启动 `demo.launch.py` (MoveGroup + RViz + ros2_control) + `arm_bridge.py` (串口桥接)

### 结果
- arm_bridge 连接 /dev/ttyACM0 OK
- /joint_states 订阅正常
- RViz Plan & Execute → 真实舵机同步

### 命令
```bash
# 终端 1
ros2 launch elite_test demo.launch.py
# 终端 2
ros2 run elite_test arm_bridge.py
```

---

## 新增: robot_brain 智能控制包 (2026-05-25)

### 功能
语音→视觉→豆包 Vision Pro 决策→语音反馈→机械臂执行 的完整管线。

### 文件结构
```
robot_ws/src/robot_brain/
├── CMakeLists.txt
├── package.xml
├── config/api_config.yaml          # API 密钥和参数
├── launch/brain_pipeline.launch.py  # 启动文件
└── scripts/
    ├── doubao_client.py             # 豆包 Vision Pro API 封装
    └── brain_pipeline.py            # 主管线: 摄像→AI→串口→舵机
```

### 数据流
```
麦克风(语音) → ASR → 文字指令
                         ↓
摄像头(图像) → brain_pipeline → doubao-1.5-vision-pro-32k
                         ↓                    ↓
                    决策 JSON           语音回复
                    ↓                   ↓
              机械臂串口指令          TTS 语音合成
```

### 使用方法
```bash
ros2 run robot_brain brain_pipeline.py --ros-args -p api_key:=YOUR_VOLCENGINE_KEY

# 交互终端输入指令:
> 抓取左边那个红色的物体
> 把东西放到右边
> 挥挥手
```

### 预定义动作
- `home`: 归位 (90°)
- `grasp`: 抓取 (闭合夹爪)
- `place`: 放置 (张开)
- `wave`: 挥手
- `left`/`right`: 左右旋转

### 依赖模型
- doubao-1.5-vision-pro-32k (火山引擎)

---

## 新增: 双目摄像头瓶子识别+测距 (2026-06-02)

### 问题
需要识别矿泉水瓶并获取空间距离，用于机械臂抓取。

### 实现
`robot_brain/scripts/stereo_detect.py` — 基于 PDF 教程实现:
1. 双目摄像头图像分割 (640×480 → 320×240×2)
2. SGBM 立体匹配 → 视差图 → 距离图
3. HSV 颜色检测 (白色瓶身 + 蓝色瓶盖)
4. 距离 = (焦距 × 基线) / 视差

### 文件
- `robot_brain/scripts/stereo_detect.py`

### 使用方法
```bash
python3 stereo_detect.py 1  # 摄像头 ID
```

### 待完成
双目摄像头标定 → 获取精确内参和基线

### 参考
Share/双目摄像头教程.pdf (29页)

---

## 补充: YOLO + 双目检测集成 (2026-06-07)

### 问题
需要 YOLO 识别物体 + 双目测距。

### 调试过程
1. pip install ultralytics → 网络太慢失败
2. 尝试 YOLOv5 torch.hub → 触发 ultralytics 依赖 → pip 死循环
3. 修补 hubconf.py + common.py 去除 ultralytics import
4. 最终用 OpenCV DNN + YOLOv3-tiny (权重手动下载)
5. YOLOv8 ONNX 尝试 → GitHub 404，下载失败
6. 终版: OpenCV DNN 自动选择 yolov4-tiny > yolov3-tiny > mobilenet-ssd

### 文件
- `robot_brain/scripts/stereo_detect.py` — 纯 HSV 检测版
- `robot_brain/scripts/yolo_stereo_detect.py` — YOLO DNN + SGBM 测距

### 关键经验
- 国内网络环境 pip 大包安装极慢，优先选 OpenCV DNN（零 pip 依赖）
- 模型权重手动下载放 /tmp/，代码自动检测

---

## 补充: 导航调试 (2026-06-09)

### 问题
1. odom 无输入时持续漂移 → 地图上下跳动
2. local_costmap 显示 "no map received"
3. lifecycle_manager 反复超时 "Failed to bring up"
4. YDLIDAR 大量 Check Sum 错误
5. serial_odom 报 "multiple access on port"
6. controller_server 始终 inactive

### 修复
| 问题 | 修复 | 文件 |
|------|------|------|
| odom 编码器噪声漂移 | 死区过滤 (<0.005归零) + 位置保持 | serial_odom_receiver.py |
| lifecycle 超时 | service_timeout: 15, bond_timeout: 10.0 | nav2_params.yaml |
| 串口冲突 | arm_bridge 和 serial_odom 互斥 | 进程管理 |

### 根因
VirtualBox USB 直通不稳定，双路高速串口（激光+STM32）同时工作导致数据损坏。代码修复在真实 RDK X5 上验证。

### 死区 + 位置保持代码
```python
VEL_DEADBAND = 0.005
if abs(linear_velocity_x) < VEL_DEADBAND: linear_velocity_x = 0.0
if abs(angular_velocity_z) < VEL_DEADBAND: angular_velocity_z = 0.0
# 速度为零时锁住位置
if linear_velocity_x == 0.0 and angular_velocity_z == 0.0:
    position_x, position_y = self.last_x, self.last_y
```

---

## 补充: RViz 控制启动 (2026-06-09)

### 命令
```bash
# 导航全栈
ros2 launch alloy_chassis_mecanum navigation_with_robot.launch.py

# 机械臂 RViz 控制
ros2 launch elite_test demo.launch.py
ros2 run elite_test arm_bridge.py

# 双目 + YOLO
/home/cobrander/RDK_X5/install/robot_brain/lib/robot_brain/yolo_stereo_detect.py 2

# 抓取测试
ros2 run elite_test arm_fixed_grasp_test.py
```

### 当前状态
- 导航栈: 代码完成，VM USB 不稳定待移植 RDK X5
- 机械臂: 完全可用（RViz + 串口桥接 + PCA9685）
- 双目测距: 可用（需手动下载模型权重）
- STM32 固件: 完成
